"use strict";

//Чему будут равны переменные a, b, c и d в примере ниже?

let a = 1; // a = 2 (увеличена с помощью инкремента в префиксной форме)
let b = 1; // b = 2 (увеличена с помощью инкремента в постфиксной форме)

let c = ++a; // с = 2
let d = b++; // d = 1
